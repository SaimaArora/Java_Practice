class Solution {
    public int hIndex(int[] citations) {
        int n = citations.length;
        Arrays.sort(citations);
        for(int i = 0; i < n; i++) {
            int leftover = n - i;
            if(citations[i] >= leftover) return leftover;
        }
        return 0;
    }
}
