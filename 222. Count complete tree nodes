class Solution {
    int count = 0;
    private void count(TreeNode root) {
        if(root == null) return;
        count++;
        count(root.left);
        count(root.right);
    }
    public int countNodes(TreeNode root) {
        count(root);
        return count;
    }
}
