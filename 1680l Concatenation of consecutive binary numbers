class Solution {
    public int concatenatedBinary(int n) {
        int mod = 1000000007, len = 0;
        long res = 0l;
        for(int i = 1; i <= n; i++) {
            if((i&(i - 1)) == 0) len++;
            res = ((res << len)+i) %mod;
        }
        return (int) res;
    }
}
