class Solution {
    public boolean canJump(int[] nums) {
        int find=0,n=nums.length;
        for(int i=0;i<n;i++){
            if(i>find) return false;
            int jump=i+nums[i];
            find=Math.max(find,jump);
        }
        return true;
    }
}
